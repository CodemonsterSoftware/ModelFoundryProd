from django import forms


# All module parameter handling is now done through the dynamic module framework
# (api_module_run + job.json). Django Forms are no longer used for module submissions.
#
# This file is kept as a placeholder for any future admin or non-module forms.
